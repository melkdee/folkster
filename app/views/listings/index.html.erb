

	<!--CONTAINER WRAPPER-->
	<div class="container">
		<!--PAGINATION-->
		
		<p><%= pagy_nav(@pagy).html_safe %></p>

		<div class="card-columns">
	
	<!--SHOW LISTINGS-->
	<% @listings.each do |listing| %>
		<div class="card mycard">
		    <h2 class="pink"><%= listing.name %></h2>
		    <p class="lightweight"><%= listing.description %></p>
		    <small class="text-muted"><%= listing.address %></small>
		    <!--<div class="card-footer">
      		<small class="text-muted">#TAG01 #TAG02</small>
    		</div>-->
		</div>

	<% end %>

		</div><!--/card-columns-->
	</div><!--/container-->











